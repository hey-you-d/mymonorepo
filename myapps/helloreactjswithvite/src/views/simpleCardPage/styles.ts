import styled from "styled-components";

export const CheckoutTable = styled.div`
    display: grid;
    gap: 0;
    border: none;
    width: fit-content;
    grid-template-columns: 60% 20% 20%;
`;

export const CheckoutCell = styled.div`
    margin: 0;
    padding: 8px 12px;
`;
